name: crystal-bigquery
version: 0.1.0
crystal: ">= 1.6.0"

dependencies:
  connect-proxy:
    github: spider-gazelle/connect-proxy
    version: ~> 1.0
  jwt:
    github: crystal-community/jwt
    version: ~> 1.4

development_dependencies:
  webmock:
    github: manastech/webmock.cr
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.3

license: MIT
